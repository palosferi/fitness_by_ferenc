# fail2ban filter for the fitness dashboard's basic-auth failures.
# Matches the line app.py logs on every rejected login attempt:
#   2026-09-15 08:12:44,001 WARNING dashboard auth failure from 1.2.3.4 (attempt 3)
[Definition]
failregex = dashboard auth failure from <HOST>
ignoreregex =
